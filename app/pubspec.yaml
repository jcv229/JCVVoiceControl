name: jcv_voice_control
description: "JCV Voice Control Android 100% local pour personne aveugle — appels, SMS, WhatsApp, perception, navigation."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_tts: 4.0.2
  permission_handler: 11.3.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
